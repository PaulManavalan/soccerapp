# Keep this value private. It must exactly match the Supabase Edge Function secret.
DUEL_ANALYSIS_WORKER_SECRET=replace-with-a-long-random-value

# Create this in the OpenAI platform. Never expose it in browser code or Git.
OPENAI_API_KEY=

# Change only after testing another vision-capable model.
OPENAI_MODEL=gpt-4.1-mini
